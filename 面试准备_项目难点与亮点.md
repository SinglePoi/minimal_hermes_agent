# 面试准备：项目难点与亮点归纳

> 用途：拿 `D:\chatbot\minimal_agent`（通用 Agent 骨架）面试时，组织自我介绍、项目深挖和追问回答。

---

## 一、一句话定位

这是一个**从零实现的通用 Agent 骨架**，架构对齐开源 Hermes Agent，但技术栈更轻：Python + DeepSeek（OpenAI 兼容接口）+ SQLite + 标准库 HTTP + 原生前端。

核心能力是一条完整的闭环，而不是「调 API 聊天」：

- Agent Loop（调模型 → 工具调用 → 执行 → 回传 → 再问模型 → 最终回答）
- 工具系统 + 工具并行调度 + 中断语义
- 三层记忆与召回（文件记忆 / 可插拔 provider / 会话检索）
- 上下文压缩
- 危险命令审批与敏感信息脱敏
- Skills 按需加载
- HTTP 服务化 + Web 前端 + SSE 流式

核心代码在 `minimal_agent.py`（约 2700 行，Agent 主循环），并拆出了 `tool_dispatch.py`（并发调度）、`context_compressor.py`（上下文压缩）、`approval.py`（审批）、`memory_manager.py`（记忆编排）、`redact.py`（脱敏）、`server.py`（HTTP 服务）等模块。

---

## 二、亮点

### 1. 完整的 Agent 架构，不是 Demo

- 自己实现了完整 Agent Loop，并有 turn 级预算控制：`MAX_AGENT_TURNS` / `TURN_TOKEN_BUDGET`。
- 触顶后不带工具再调一次模型，请求基于已有信息给出最终回答。
- 说明理解 Agent 不只是 prompt，而是状态机 + 调度 + 预算 + 终止条件。

### 2. 安全体系（最硬的加分项）

- 危险命令审批：terminal 执行前过审批门卫，分「硬性禁止 → 危险模式检测 → 交互授权（once/session/always/deny）→ 用户自定义 deny 规则」多层。
- smart 审批 + 熔断：辅助 LLM 先评估命令，连续拒绝达到阈值触发 CIRCUIT BREAKER 硬停，人工批准才重置。
- tirith 内容级扫描：识别 ANSI 转义/控制字符注入、零宽字符、双向覆盖符、同形字域名（钓鱼）、管道到解释器等语义威胁。
- 全链路脱敏：密钥、JWT、私钥块、手机号、DB 连接串、URL 查询参数统一打码；不可复用哨兵防止模型把打码值写回文件。
- SSRF 防护：联网工具只放行公网 http/https，拒绝 localhost / 私网 / 链路本地。

### 3. 工具并行调度 + 中断语义

- 把模型一轮内的多个工具调用切成 parallel / sequential 段。
- 只读白名单并发，有副作用工具按屏障串行。
- 同路径读写的重叠检测：写同一文件排队，保证语义等价于串行。
- 结果按模型原始顺序回填。
- 中断时取消 pending future、给运行中工具 3 秒优雅退出、terminal 用 `taskkill /T` 杀进程树。

### 4. 上下文压缩

- 超过 50% 上下文窗口触发，把中间轮次交给 LLM 生成交接摘要，保留最近 N 条完整消息。
- 处理了三个容易被忽视的坑：
  - `merge-into-tail` 避免 user→user 交替错误；
  - 技能内容被压掉后用 `[SKILL_PRUNED]` 标记防止幽灵技能；
  - 压缩边界先落库记忆、保留未完成 todo 清单，任务状态跨压缩不丢。

### 5. 记忆系统有抽象分层

- 三层记忆：`MEMORY.md` / `USER.md` 文件记忆 + 可插拔 `MemoryProvider` 插件 + SQLite FTS5 会话检索。
- LLM 提取事实后再入库，而不是存用户原话。
- 异步同步 + 最新任务覆盖旧任务的合并节流，避免阻塞对话。
- 向量检索后端可切换（tfidf / local / api）。

### 6. 工程化程度高

- 33 个测试文件、211 个测试用例，覆盖审批、工具分发、脱敏、压缩、服务端、会话导出等。
- 依赖极简：openai / python-dotenv / rich；docx/xlsx/ipynb 抽取、ANSI 剥离、SQLite FTS5、标准库 HTTP server 均自己实现。
- LLM 调用带指数退避 + 随机抖动重试，区分可重试（429/5xx/超时）与不可重试（400/401/403/404）。
- 鉴权 + 操作审计：HMAC 常量时间比较、scrypt 密码哈希、HMAC 会话 cookie、防时序侧信道、JSON 审计日志。

---

## 三、难点

建议面试时挑 2–3 个深挖，按「为什么难 → 怎么解决」展开。

### 1. 上下文窗口有限 vs 长对话要持续进行

**难在哪**：LLM 窗口是硬约束，长对话会爆；简单截断会丢用户画像、任务进度、已加载技能指令，导致模型失忆或重复干活。

**怎么解决**：不做删除，做「压缩 + 归档」。中间轮次交给 LLM 生成权威交接摘要，保留最近 N 条原文，原始消息仍落 SQLite。核心取舍是判断哪些信息必须逐字保留、哪些可以安全变成摘要。

### 2. 工具并行提速与「语义等价于串行」的矛盾

**难在哪**：直接并发跑最快，但 memory / terminal / todo 有副作用、多个文件工具可能写同一路径，乱序会破坏一致性；同时还要支持用户中断时优雅取消。

**怎么解决**：静态区分只读无状态与有副作用，只读进白名单并发；路径作用域工具做子树重叠检测，含写者的重叠降级为屏障；结果按原始顺序回填，保证与串行完全一致。中断用轮询 + 取消 pending future + 3s grace。

### 3. 给 LLM 一个 shell 却要守住安全底线

**难在哪**：完全禁掉 terminal 会削弱 Agent 能力，放开又可能被诱导执行 `rm -rf /`。静态正则漏报严重，命令混淆（`base64 -d | bash`、`eval $(curl)`）和 prompt injection 都能绕过。

**怎么解决**：分层防御——硬性禁止地板 → 危险模式 + 用户 deny glob（优先级最高）→ tirith 内容级扫描 → smart 审批 + 连续拒绝熔断。命令先剥注释、包在定界符里，明确要求忽略命令内夹带指令。

### 4. 敏感信息在「模型 ↔ 文件 ↔ 前端」整条链路都不能漏

**难在哪**：打码太狠会破坏工具语义（如 `token_count` 不能误伤），打码太松会泄露；模型可能把打码占位符再写回文件，造成二次污染。

**怎么解决**：统一走 `redact_sensitive_text` 管线；read_file 读敏感文件是打码后读取；用不可复用哨兵防止复述；URL 参数用精确键匹配避免误伤。

### 5. 记忆持久化的并发与恢复一致性

**难在哪**：用户快速连发多轮时同步写盘会阻塞；多个写入任务竞争会产生脏数据；恢复会话时要让 nudge 轮次计数对齐历史。

**怎么解决**：同步交给单线程后台 worker 立即返回，未开始跑的旧任务被最新任务覆盖（messages 是全量历史，不丢数据）；退出时 flush 有界等待 10s；恢复时按历史轮次水合计数。

---

## 四、面试讲述建议

### 开场 30 秒

先给定位：这是一个通用 Agent 骨架，对齐 Hermes 架构，核心解决 LLM 应用里的工具调用、记忆、上下文压缩和安全审批这几个工程问题。技术栈是 Python + DeepSeek/OpenAI 兼容接口 + SQLite + 标准库 HTTP + 原生前端。

### 深挖顺序

建议主动带一个最有把握的难点，比如「工具并行调度的一致性」或「上下文压缩」，用 STAR 结构讲：当时遇到的问题 → 我怎么做 → 最终效果 / 权衡。不要只念功能清单。

### 可能被追问的点

- 为什么对齐 Hermes 而不直接用它的框架？
  - 答：通过自己实现来理解它的设计取舍，同时在更小代码量下验证关键机制；README 里每处都标注了对齐了 Hermes 哪个文件。
- 单文件 2700 行是不是设计问题？
  - 答：早期快速迭代，后期已把并发调度、压缩、审批、记忆、脱敏拆成独立模块，主文件保留 Agent 主循环和会话管理，这是一个自然的可接续重构话题。
- 安全审批会不会误拦截 / 漏拦截？
  - 答：讲清楚分层 + 熔断 + 用户 deny 优先，并诚实说明当前是简化版（没有 Hermes 的 checkpoint 系统），讲清边界。
- 测试怎么保证质量？
  - 答：211 个测试、覆盖核心模块，LLM 相关用 mock/fake。

---

## 五、关键模块索引

- `minimal_agent.py`：Agent 主循环、系统提示词、记忆工具、会话库（FTS5）、LLM 重试、终端工具、turn 预算、REPL。
- `tool_dispatch.py`：工具并行/串行分段、路径重叠检测、中断执行。
- `context_compressor.py`：上下文压缩、交接摘要、技能裁剪标记。
- `approval.py`：危险命令审批、smart 审批、熔断、允许列表。
- `redact.py`：敏感文本脱敏管线。
- `memory_manager.py`：记忆 provider 编排、异步同步、合并节流。
- `server.py`：HTTP 服务、SSE 流式、鉴权、审计。
- `web/`：原生 HTML/CSS/JS 前端。
- `tests/`：33 个测试文件、211 个测试用例。
